# Installation

## Plugin Store

You can install **Maps** from the [Craft Plugin Store](https://plugins.craftcms.com) inside your Craft admin. Simply search for "Maps" and click the icon.

## Composer

To install via command-line, execute the following from inside your project's root directory:

```bash
composer require sup/craft-geo

./craft install/plugin geo
```
